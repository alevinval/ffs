use crate::{name::Name, node::Node, tree::TreeNode};

pub const MAX_FILE_SIZE: usize = Node::MAX_FILE_SIZE;
pub const FILENAME_LEN: usize = Name::LEN;
pub const DIR_NODE_ENTRIES: usize = TreeNode::LEN;
